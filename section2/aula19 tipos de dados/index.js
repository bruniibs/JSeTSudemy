const nome1 = 'Luiz'; // string 1
const nome2 = `Luizinho`; // string 2 > pode criar template strings dentro dela
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined = não aponta local nenhum na memoria TEM Q SER LET
const sobrenomeAluno = null; // nulo -> não aponta local nenhum na memoria // NÃO SAO IGUAIS
// quando usado nulo vc expecifica que a var não aponta nenhum local na memoria
const boolean = true; //true or false. boolean padrão